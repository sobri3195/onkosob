import {ExternalLink,Trash2} from 'lucide-react'
export function SavedPageCard({title,subtitle,note,onNote,onOpen,onDelete,category,onCategory,categories}:{title:string;subtitle:string;note?:string;onNote:(v:string)=>void;onOpen?:()=>void;onDelete:()=>void;category?:string;onCategory?:(v:string)=>void;categories?:readonly string[]}){return <article className="saved-card"><div className="saved-top"><div><strong>{title}</strong><small>{subtitle}</small></div><div>{onOpen&&<button aria-label="Buka sumber" onClick={onOpen}><ExternalLink/></button>}<button aria-label="Hapus" onClick={onDelete}><Trash2/></button></div></div>{categories&&<select aria-label="Kategori" value={category||categories[0]} onChange={e=>onCategory?.(e.target.value)}>{categories.map(x=><option key={x}>{x}</option>)}</select>}<textarea aria-label={`Catatan untuk ${title}`} placeholder="Tambah catatan…" defaultValue={note} onBlur={e=>onNote(e.target.value)}/></article>}
