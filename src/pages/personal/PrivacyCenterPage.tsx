import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useUser } from '@/context/UserContext'
import { clearCompanionData } from '@/hooks/useCompanionData'
import { PersonalPage } from './ProfilePage'
export default function PrivacyCenterPage() { const { reset, download } = useUser(); const [confirm, setConfirm] = useState(false); const navigate = useNavigate(); return <PersonalPage eyebrow='PUSAT PRIVASI' title='Data Anda tetap dekat dengan Anda.' lead='Tanpa akun, personalisasi disimpan hanya di perangkat. Dengan akun, data yang Anda pilih dapat disinkronkan secara aman ke backend Lentera.'><div className='privacy-grid'><section><h2>Yang disimpan</h2><ul><li>Nama tampilan opsional dan preferensi peran</li><li>Tahap perjalanan, progres belajar, pertanyaan, dan checklist</li><li>Dokumen yang Anda unggah secara sadar ke Document Vault pribadi</li></ul></section><section><h2>Document Vault</h2><ul><li>Bucket file bersifat privat; pratinjau dan unduhan memakai akses sementara</li><li>Anda mengontrol penghapusan, dan berbagi selalu harus eksplisit</li><li>Lentera bukan penyimpanan rekam medis resmi dan tidak membaca isi laporan</li></ul></section></div><section className='privacy-explain'><h2>Jangan jadikan Lentera satu-satunya salinan</h2><p>Simpan juga dokumen penting melalui saluran resmi atau penyimpanan pribadi yang Anda percaya. Penghapusan di Lentera tidak menghapus salinan pada fasilitas kesehatan.</p><button onClick={download}>Unduh Data Saya</button><button className='danger-link' onClick={() => setConfirm(true)}>Hapus Semua Data di Perangkat Ini</button>{confirm && <div className='confirm-box' role='alertdialog' aria-labelledby='privacy-delete'><h3 id='privacy-delete'>Hapus semua data lokal?</h3><p>Tindakan ini menghapus personalisasi lokal. Penghapusan akun terautentikasi juga menghapus file vault, metadata, dan izin berbagi melalui proses server.</p><button onClick={() => setConfirm(false)}>Batal</button><button className='danger' onClick={() => { reset(); navigate('/') }}>Hapus Data</button></div>}</section></PersonalPage> }
